--- # Test playbook for LOOPS..
- hosts: 127.0.0.1
  user: ansible
  become: yes
  become_method: sudo
  connection: ssh
  gather_facts: no
  
  tasks:
  - name: Creating Users using LOOP
    user: name={{ item }} state=present
    with_items:
         - user4
         - user5
         - user6
